# Workshop Progress

This directory stores the persistent progress state for learning workshops that span multiple sessions.

Each dated file tracks the status of every learning object in a workshop as `Outstanding`, `Persisted`, `Skipped`, or `Deferred`. This allows the Learning Coach to resume a workshop across separate conversations without repeating completed topics.

These files are internal processing artifacts for Agent coordination and are not part of the permanent learning resource.
